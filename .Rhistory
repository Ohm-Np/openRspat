library(openRspat)
library(openRspat)
area_computation <- function(roi, data, unit) {
# transform crs of pol to area_proj()
roi <- st_transform(roi,
area_proj(roi))
# transform crs of vec to match with polygon
data <- st_transform(data,
st_crs(roi))
# perform intersection
intersected <-
st_intersection(data,
roi)
if (unit=="km") {
# compute area
intersected$area_sqkm <-
as.character(st_area(intersected)/1000000)
} else {
# compute area
intersected$area_sqm <-
as.character(st_area(intersected))
}
# drop geometry
area_stats <- st_drop_geometry(intersected)
# return results
return(area_stats)
}
area_computation <- function(x) {
}
library(openRspat)
library(openRspat)
library(openRspat)
library(openRspat)
library(openRspat)
library(openRspat)
fname <- system.file("shape/nc.shp", package="sf")
sf <- sf::st_read(fname)
get_accessibility("5k_10k", 14189840, "../")
openRspat::get_accessibility("5k_10k", 14189840, "../")
acc <- terra::rast("../5k_10k.tif")
acc.c <- terra::crop(acc, sf)
plot(acc.c)
fname <- system.file("shape/nc.shp", package="sf")
sf <- sf::st_read(fname)
View(sf)
library(openRspat)
